def count_by(x, n):
    result = []

    for i in range(1, n + 1):
        result.append(x * i)

    return result

powers of two

def powers_of_two(n):
    result = []

    for exponent in range(n + 1):
        result.append(2 ** exponent)

    return result 
